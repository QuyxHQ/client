import { Items, UserInfo, UserTop } from "./components";

const User = () => {
  return (
    <section>
      <UserTop />
      <UserInfo />
      <Items />
    </section>
  );
};

export default User;
